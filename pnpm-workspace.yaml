# Single-app repo — declare `packages` so `pnpm store path`, used by
# actions/setup-node's cache, does not error out on the empty workspace.
packages:
  - .
allowBuilds:
  core-js: false
  sharp: false
  unrs-resolver: false
minimumReleaseAgeExclude:
  - '@0xsarwagya/clinical-receipt'
